# Ads Setup

Search for `ad-slot` in `index.html` and paste ad code inside those sections after your ad network account is approved.

Rules:

- Do not make ads look like scan buttons.
- Do not make ads look like warning messages.
- Do not use fake download buttons.
- Do not place ads in a way that tricks users into clicking.
